import HeroParticlesCanvas from './HeroParticlesCanvas';

/** Lightweight hero background — Canvas 2D (no Three.js dependency). */
export default function HeroParticles() {
  return <HeroParticlesCanvas />;
}
